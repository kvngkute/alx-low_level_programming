#include <stdio.h>
/**
 * main - this is the main function or starting point of our program
 *
 * Return: this returns 0 when the program is successful
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
