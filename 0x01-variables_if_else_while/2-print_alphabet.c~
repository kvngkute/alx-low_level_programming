#include <stdio.h>

/**
 * main - This is the main function or entry point
 * Return: Returns 0 when succesful
 */
int main(void)
{
char i = 'a';
for (i = 'a' ; i <= 'z' ; i++)
putchar(i);
putchar("\n");
return (0);
}
