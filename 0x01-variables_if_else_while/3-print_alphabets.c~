#include <stdio.h>
/**
 * main - This program prints alphabets in lowercase and uppercase
 * Return: Returns 0 whwen succesful
 */
int main(void)
{
char i;
for (i = 'a' ; i <= 'z' ; i++)
putchar(i);
for (i = 'A'; i <= 'Z' ; i++)
putchar(i);
putchar('\n');
return(0);
}
