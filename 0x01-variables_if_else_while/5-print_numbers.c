#include <stdio.h>
/**
 * main - This program prints all single digits of base 10
 * Return: This program returns 0 when succesful
 */
int main(void)
{
int i;
for (i = 0 ; i < 10 ; i++)
printf("%d", i);
printf("\n");
return (0);
}
